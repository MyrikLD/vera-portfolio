# Public site URL (used to fix OAuth redirect_uri behind reverse proxy)
PUBLIC_SITE_URL=https://vera.myrik.xyz

# Keystatic GitHub OAuth (set in GitHub Actions secrets, not locally needed for dev)
PUBLIC_KEYSTATIC_GITHUB_CLIENT_ID=
KEYSTATIC_GITHUB_CLIENT_SECRET=
KEYSTATIC_SECRET=
